# Skill Archetype Specification

> **Canonical source**: `~/.claude/skills/docs/skill-archetype-spec.md`

This file moved to the skills repo — the root of the truth chain.

The skills repo is the source of truth for all council governance. steaz.cloud reads downstream via `build-skills.ts`.
